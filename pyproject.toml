[project]
name = "study-agent"
version = "0.1.0"
description = "Hebrew AI study agent for computer networking courses with RAG and subnet calculations"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chromadb>=1.5.9",
    "openai>=3.2.0",
    "python-bidi>=0.6.11",
    "python-dotenv>=1.2.3",
    "streamlit>=1.61.1",
]
